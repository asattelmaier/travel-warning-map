export const environment = {
    production: true,
    // This placeholder is replaced by the 'replace-api-url' script during build (see package.json)
    travelWarningsApiBase: 'TRAVEL_WARNINGS_API_URL'
};
